fastapi==0.115.0
uvicorn[standard]==0.32.0
pymongo==4.10.1
pydantic==2.9.2
python-multipart==0.0.20
httpx==0.27.2
anthropic==0.49.0          # ROADMAP §11.10 · 升級拿 Opus 4.7 1M context + prompt caching v2

# Auth + rate limit + logging
PyJWT==2.10.0
slowapi==0.1.9
bcrypt==4.2.1              # v1.3 User Management · 對齊 LibreChat 10-round hash

# V1.1 §E-2 · 多來源知識庫抽字 + Meili
pymupdf==1.25.5           # Audit · dep #1 · 1.24.x PDF parser memory CVE 已修
python-docx==1.1.2        # .docx
python-pptx==1.0.2        # .pptx
openpyxl==3.1.5           # .xlsx / .xls
Pillow==11.0.0            # 圖片 EXIF + 尺寸
pillow-heif==0.18.0       # Day 2.3 · iPhone HEIC 自動轉 JPEG(場勘 PWA)
meilisearch==0.33.0       # 全文搜尋 client
openai==1.55.3            # Feature #1 · Whisper STT(會議速記) + gpt-image-2 SDK(選配 · 目前 design 直接 httpx)

# v1.66 Q3 · 監控 · 異常自動上報
sentry-sdk[fastapi]==2.18.0  # Sentry · DSN 沒設則 no-op · 不影響本地 dev

# 測試(選配)
pytest==8.3.3
mongomock==4.2.0.post1
