{
  "name": "company-ai-workspace",
  "version": "1.4.1",
  "private": true,
  "scripts": {
    "typecheck": "npm run typecheck --prefix desktop",
    "test": "cd backend/accounting && pytest tests/ test_main.py -q --tb=short --ignore=tests/integration",
    "build": "npm run build --prefix frontend/launcher"
  }
}
